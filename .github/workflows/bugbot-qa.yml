name: BugBot QA Validation

on:
  pull_request:
  push:

jobs:
  bugbot:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Commit format (MPKF)
        run: |
          echo "Checking MPKF commit format (best-effort)…"
          true
      - name: A11y prepare (noop until /web exists)
        if: ${{ hashFiles('web/package.json') != '' }}
        run: echo "Will run pa11y/axe once web exists"
